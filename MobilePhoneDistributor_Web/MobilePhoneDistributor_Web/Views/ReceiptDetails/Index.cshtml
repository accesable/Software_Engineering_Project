@model IEnumerable<MobilePhoneDistributor_Web.Models.ReceiptDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PhoneModel.PhoneName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Receipt.StaffId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UnitAmmount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneModel.PhoneName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Receipt.StaffId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UnitAmmount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReceiptDetailId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReceiptDetailId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReceiptDetailId })
        </td>
    </tr>
}

</table>
